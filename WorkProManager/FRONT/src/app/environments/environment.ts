// src/environments/environment.ts
export const environment = {
  production: false,          // Indica si es un entorno de producción
  apiUrl: 'http://localhost:3001', // URL del servidor backend en desarrollo
  secretKey: 'pepito123',     // Clave secreta para JWT, o cualquier otra clave que quieras definir (reemplaza por algo más seguro)
};
